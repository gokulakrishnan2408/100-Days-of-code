#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
    float t,n,m,k;
    cin>>t;
    while(t--){
        cin>>n>>m>>k;
        if(n/k <= m){
            cout<<"Yes"<<endl;
        }
        else{
            cout<<"No"<<endl;
        }
    }
    return 0;
}
